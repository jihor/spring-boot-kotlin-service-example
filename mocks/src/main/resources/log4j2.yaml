Configuration:

  Appenders:
    Console:
      name: STDOUT
      PatternLayout:
        Pattern: "%clr{%d}{faint} %clr{[%15.15t]}{faint} %clr{%-40.40c{1.}}{cyan} %clr{:}{faint} %m%n"

  Loggers:
    Root:
      level: INFO
      AppenderRef:
        ref: STDOUT